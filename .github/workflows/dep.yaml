name: Dependabot
on:
  push:
    branches:
      - 'main'

jobs:
  ci-image:
    name: Changeset
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v3
      - name: Configure git
        run: |
          git checkout ${{ github.event.pull_request.head.ref }}
